## 1. Goal
Merge the supplied defaults into [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) while preserving existing custom behavior.

## 2. Approach
Keep the explicit `keybinds clear-defaults=true` strategy already used in [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) and merge the supplied bindings into it. This makes conflict resolution explicit and avoids inheriting default shortcuts that override personal choices. Retain existing shared-mode scopes, including shortcuts deliberately available in locked mode.

## 3. File Changes
- **Modify** [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F): merge missing bindings, plugins and option documentation; preserve active settings. No other files are required.

## 4. Implementation Steps
### Task 1: Merge keybindings
In [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F):
1. Replace the stale autogenerated backup header with a brief explanation that defaults are explicitly merged and personal shortcuts take precedence.
2. Preserve pane `z` for fullscreen and `Z` for frames. Add default `f` fullscreen, `Shift f` fullscreen without UI, `;` previous pane, `s` stacked pane and `i` pin pane actions.
3. Preserve `Alt+p` for floating panes, `Alt+Shift+[` / `]` for tabs, shared resize/layout actions, shifted-arrow navigation, and the scroll-mode navigation actions that return to normal mode. Normalize the extra space in `Alt Shift  down`.
4. Add `Alt+f` for floating panes, unshifted Alt-arrow navigation outside scroll/locked modes, and `Alt+Shift+p` for group marking. Omit the conflicting default `Alt+p` pane-group toggle; do not invent a replacement shortcut.
5. Preserve `Ctrl+x` as the tmux entry shortcut and its existing mode exclusions. Do not add the default `Ctrl+b` entry binding; retain `Ctrl+b` for paging in scroll/search and for sending byte 2 inside tmux mode.
6. Add previous/next prompt and command selection actions (`[`, `]`, `m`) to the shared scroll/search block. Add `c` to copy the last command output in scroll mode only, retaining search-mode `c` for case sensitivity.
7. Add session host/guest focus (`]`/`[`), host fullscreen (`f`), and about/share/layout-manager plugin launchers (`a`, `s`, `l`). Preserve existing plugin launcher options and return-to-normal behavior.
8. Keep search-entry and rename Enter/Escape handling and existing shared exclusions intact. Review every supplied binding against the effective per-mode map to avoid duplicate or conflicting definitions.

### Task 2: Merge plugins and settings
In [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F):
1. Add the `about` plugin alias and `zellij:link` background plugin from the supplied defaults; retain all existing aliases.
2. Add `web_client { font "monospace"; }` and retain active `default_mode "normal"`.
3. Merge the supplied commented configuration reference, including nested sessions, pane frame style, theme switching, graphics, web settings, mouse behavior, shell integration and async worker options. Keep optional examples commented, retain existing unique examples such as session naming and serialization interval, and avoid duplicate option documentation.
4. Remove the accidental Rust string terminator fragment from the supplied async-worker comment when incorporating that section.

## 5. Acceptance Criteria
- [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) parses successfully with the installed Zellij version; if supplied actions are unsupported, identify them explicitly before claiming completion.
- In [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F), pane `z` toggles fullscreen, `Z` toggles frames, and `Alt+p` toggles floating panes with the same existing mode coverage.
- In [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F), `Ctrl+x` enters tmux mode in its original modes; `Ctrl+b` remains page-up in scroll/search and sends byte 2 from tmux mode.
- In [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F), new pane, prompt-navigation, command-copy, nested-session and plugin-launch actions are present at the specified keys without changing search `c`.
- In [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F), default mode remains normal; new optional setting examples remain commented; about alias, link background plugin and monospace web font are present.
- Only [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) changes; unrelated existing iTerm changes remain untouched.

## 6. Verification Steps
After editing [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F), run `git diff --check` and review the file diff. Check `zellij setup --help`, then use `zellij --config zellij/config.kdl setup --check` if supported by the installed binary. Its executable points to Homebrew version 0.45.1, but invoking even `--version` currently fails during logging initialization under the read-only sandbox; validation requires an execution context allowing Zellij's runtime log writes.

In a disposable session using [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F), verify pane z/Z/f, floating Alt+p, custom tab navigation, Ctrl+x tmux entry, scroll/search Ctrl+b, scroll Alt-navigation returning to normal, search Enter/Escape and rename cancellation. Exercise new actions where the terminal and shell support them; check plugin launchers after restarting a session.

## 7. Risks & Mitigations
- Shared bindings in [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) cover locked mode intentionally. Preserve their scopes and inspect effective mode coverage before adding defaults.
- The supplied defaults may target functionality newer than the installed binary. Validate [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) with Zellij before marking the merge usable; do not silently drop unsupported requested features.
- Adding `zellij:link` to [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) changes session startup by loading a background plugin. Include it as explicitly supplied and verify startup after restart.
- Commented settings in [config.kdl](air-file://tcnkokfh73moktoaltmh/Users/ashokraja/code/dotfiles/zellij/config.kdl?type=file&root=%252F) are documentation, not requested preference changes. Keep them commented to avoid enabling web sharing, clipboard reads or other optional behavior accidentally.